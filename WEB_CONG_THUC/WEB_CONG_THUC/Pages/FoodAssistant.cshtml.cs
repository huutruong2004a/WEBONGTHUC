using Microsoft.AspNetCore.Mvc.RazorPages;

namespace WEB_CONG_THUC.Pages
{
    public class FoodAssistantModel : PageModel
    {
        // Có thể thêm các thuộc tính hoặc phương thức nếu cần xử lý logic phía server 
        // trước khi trang được render, nhưng với cách tiếp cận API-driven, 
        // phần lớn logic sẽ ở JavaScript.
        public void OnGet()
        {
        }
    }
}